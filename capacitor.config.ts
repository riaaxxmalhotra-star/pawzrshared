import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pawzr.app',
  appName: 'pawzr',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
