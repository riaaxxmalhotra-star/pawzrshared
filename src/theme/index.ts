// Centralized Theme System for Pawzr
// Import this file to access all theme utilities

// Colors
export { colors, roleColors, getRoleColor, getRoleLightColor } from './colors';

// Typography
export { typography, fontWeights } from './typography';

// Spacing & Layout
export { spacing, borderRadius, shadows, screenPadding, cardPadding, layout } from './spacing';

// Component Styles
export {
  buttonStyles,
  cardStyles,
  inputStyles,
  avatarStyles,
  badgeStyles,
  listItemStyles,
  emptyStateStyles,
  headerStyles,
  sectionStyles,
} from './components';
